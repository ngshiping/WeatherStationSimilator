<Page
    x:Class="C2DSimulator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:C2DSimulator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="C2D Simulator" Margin="12,0" VerticalAlignment="Center" />

        <TextBox Name="messageBox" Grid.Row="1" Margin="12" AcceptsReturn="True" />

        <Button Name="sendBtn" Content="Send" Grid.Row="2" HorizontalAlignment="Stretch" Height="48"
                Click="sendBtn_Click" />
    </Grid>
</Page>
